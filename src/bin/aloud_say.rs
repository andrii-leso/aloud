// Placeholder. Cargo.toml declares the `aloud-say` [[bin]] target for a
// later task (the CLI/pipeline entry point); this stub exists only so the
// crate's manifest resolves and `cargo test`/`cargo build` succeed before
// that task lands. Not part of Task 1's scope.
fn main() {}
