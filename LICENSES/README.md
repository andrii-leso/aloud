# Third-party licences

What is in here, what each file covers, and — more importantly — what is still
missing. Hard constraint 3 (`CLAUDE.md`) makes these obligations load-bearing
*before any release*, so an honest list of the gaps is worth more than a folder
that looks complete.

| File | Covers | Applies to |
|---|---|---|
| `supertonic-MIT.txt` | The Supertonic **code**, vendored at `src/vendor/supertonic/` | The compiled binary |
| `supertonic-weights-OpenRAIL-M.txt` | The Supertonic 3 **model weights** | The 385 MB model, fetched at runtime |
| `THIRD-PARTY.md` | **Generated.** Every Rust crate compiled into the binary — 550 components across 8 licences | The compiled binary |

## The two Supertonic licences are not the same licence

This is the mistake the project is most likely to make, so it is written down
twice. **Supertonic's code is MIT. Supertonic's weights are BigScience Open
RAIL-M.** (Aloud's *own* code is neither — it is PolyForm Noncommercial 1.0.0,
see the root `LICENSE`.) MIT is permissive;
Open RAIL-M is permissive *plus* a set of use restrictions in its Attachment A,
which a downstream distributor has to pass on. Commercial use is permitted and
royalty-free either way — the obligations are paperwork, not a fee.

`supertonic-weights-OpenRAIL-M.txt` is the verbatim text published at
<https://huggingface.co/Supertone/supertonic-3/resolve/main/LICENSE>, fetched
2026-08-11 (15,007 bytes). The model card declares `license: openrail`.

## What is still missing

**An EULA mirroring Attachment A.** Open RAIL-M requires the use restrictions to
be passed on to end users as enforceable terms. Aloud has no EULA. This is
deliberately not drafted by an engineer — it routes to Rektor. Blocks
distribution, not development.

**The weights' licence does not currently travel with the weights.** The
download procedure fetches `onnx/` and `voice_styles/` only, so a local model
directory ends up with no `LICENSE` in it. Any procedure that copies the model —
including the first-run downloader, when it is built — should copy
`supertonic-weights-OpenRAIL-M.txt` alongside it.

## The third-party NOTICE — done, 2026-08-16

`THIRD-PARTY.md` is generated by `cargo-about` from the real dependency graph.
**Regenerate it after any dependency change:**

```bash
cargo about generate about.hbs -o LICENSES/THIRD-PARTY.md
```

Config is `about.toml` at the repo root; read its comments before adding a
licence to the accepted list.

**One finding worth carrying forward: 17 crates are MPL-2.0**, which is
file-level copyleft rather than attribution-only — the symphonia audio decoders
(via `rodio`), `cssparser`/`selectors`/`dtoa-short` (via Tauri's webview) and
`option-ext` (via `dirs`). Distributing them inside Aloud is expressly permitted
by MPL-2.0 §3.3, and Aloud uses all of them **unmodified**, so linking upstream
discharges the source-availability obligation. **If anyone ever vendors and edits
one, that changes** — the modifications must then be published under MPL-2.0.

Two things `cargo-about` structurally cannot see, both named at the top of the
generated file: **ONNX Runtime** (MIT, a prebuilt static library fetched by
`ort-sys` at build time, not a crates.io dependency) and the **model weights**
(covered above, and not part of the binary at all).
